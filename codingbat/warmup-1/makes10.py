def makes10(a, b):
  return ( (a == 10) or (b == 10) or ((a + b) == 10) )
  # `return ()` evaluates and returns the results of the conditional statement.
  # in this case, if any of the conditionals within the outer () are true, it will return True
